{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_runner",
            "type": "shell",
            "command": "pub run build_runner watch"
        },
        {
            "label": "pub upgrade",
            "type": "shell",
            "command": "pub upgrade"
        }
    ]
}
